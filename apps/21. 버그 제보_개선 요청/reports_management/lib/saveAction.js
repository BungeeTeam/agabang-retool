await updateQuery.trigger();
getReportsQuery.trigger();

// 상태 변경시 이메일 발송 모달 띄우다가 실패
//  //  modalFrame1.show();

//  const changedArr = getReportsQuery.data.map(i=>i.id === table3.changesetArray[0].id)

//  console.log(changedArr)

//  varChangedArray.setValue(changedArr)
